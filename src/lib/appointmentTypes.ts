export type Appointment = {
  id: number;
  candidate: string;
  email: string;
  role: string;
  time: string;
  date: string;
};

export type TimeOption = {
  hour: string;
  minute: string;
  period: string;
};
